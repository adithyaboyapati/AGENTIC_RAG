# Development / CI tooling. Pinned for the same reason as requirements.txt:
# an unpinned linter or scanner turns CI red on someone else's release day.
-r requirements.txt

# Lint
ruff==0.16.5

# Test
pytest-cov==7.1.0
pytest-timeout==2.5.0

# Security scanning
pip-audit==2.10.1

# Locust is not installed here. 2.42.1 is the last release that supports
# Python 3.10 and it requires pytest<9, which conflicts with pytest==9.1.1.
# Load tests: pip install -r requirements-load.txt
