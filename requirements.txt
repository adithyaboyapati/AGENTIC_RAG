# Pinned to the versions validated in development (conda env bapi_lang).
# Update deliberately: bump a pin, run the test suite, then commit.

# Core LangChain stack
langchain==1.3.2
langchain-core==1.4.8
langchain-text-splitters==1.1.2
langchain-openai==1.2.1
langchain-community==0.4.2
langgraph==1.2.2
langchain-chroma==1.1.0

# Vector store
chromadb==1.5.9

# PDF ingestion
pymupdf==1.28.0

# API (Phase 8)
fastapi==0.136.3
uvicorn[standard]==0.48.0

# Utilities
python-dotenv==1.2.2
pydantic==2.13.4
pydantic-settings==2.14.2
tiktoken==0.13.0
rich==14.3.4

# Evaluation (Phase 8)
ragas==0.4.3

# Web search tool (Phase 2+) — LangChain DuckDuckGoSearchRun backend
ddgs==9.14.4

# Frontend
streamlit==1.58.0

# Memory (optional Supabase persistence)
supabase==2.31.0

# Dev / test
pytest==9.1.1
pytest-asyncio==1.4.0
httpx==0.28.1
